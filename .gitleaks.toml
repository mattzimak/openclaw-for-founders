# gitleaks configuration - uses the default rule set shipped with gitleaks.
# Run locally: gitleaks protect --staged   (pre-commit)   or   gitleaks detect   (history)
title = "openclaw-field-guide"

[extend]
useDefault = true
